using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VideoAlert : MonoBehaviour
{
    public VideoAlpha videoAlpha;
    // Start is called before the first frame update
    void Start()
    {
        videoAlpha.isPlay = true;
    }
    
}
